@{ 
    @model IEnumerable<Audiology.Web.ViewModels.Songs.SongsByAlbumViewModel>
}

        <ol>
            @foreach (var song in Model)
            {
                    <li> <a asp-controller="Songs" asp-action="ById" asp-route-id="@song.Id" class="text-decoration-none"><span>@song.Name</span></a></li>
            }
        </ol>